name: student_assistant_hub
description: A Student Assistant Application for TPG316C Assignment
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  provider: ^6.1.5
  supabase_flutter: ^2.8.0
  file_picker: ^8.0.0
  image_picker: ^1.0.4
  url_launcher: ^6.2.5
  intl: ^0.19.0
  cross_file: ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true